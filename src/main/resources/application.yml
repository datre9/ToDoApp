server:
  port: 8080
app:
  name: ToDoApp
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/todo
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: create-drop